<section class="pt-2 pb-2">
    <div class="container">
      <div class="row">
        <div class="col-6">
          <h3 class="text-center mb-3">  Top rating ⭐️⭐️⭐️ </h3>
        </div>
        <div class="col-6 text-right">

        </div>
        <div class="col-12">
          <div class="cards p-4">
            <% @toprated_mascots.each do |mascot| %>
              <%= link_to mascot_path(mascot), class: 'text-light text-decoration-none' do  %>
                <div class="card-trip rounded-4">
                  <%= image_tag mascot.photo.attached? ? cl_image_path(mascot.photo.key) : 'https://mondomascots.com/wp-content/uploads/2023/01/image1-1.jpeg', class: 'rounded-3' %>
                  <h2 class="text-center pt-3"><%= mascot.name %></h2>
                  <div class="card-trip-infos">
                    <div>
                      <p><%= mascot.location %></p>
                      <p><%= mascot.average_rating %></p>
                    </div>
                    <p class="card-trip-pricing opacity-50">From ¥<%= mascot.price %>~</p>
                  </div>
                </div>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </section>
